# FoodsbyCodeChallenge

This is a simple application that reads from a JSON file and displays the data based on the date chosen by the user

Requirements:
XCode 7+
Swift 3+

Debugging: "command/usr/bin/codesign failed with exit code 1- code sign error" occurs clean the product and build again
